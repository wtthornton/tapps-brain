# direnv config — loads .env into the shell env when you `cd` here.
# Install once (already done on this box, 2026-04-17):
#   sudo apt-get install direnv
#   eval "$(direnv hook bash)"  # in ~/.bashrc
# After editing .env, run:  direnv allow
dotenv
