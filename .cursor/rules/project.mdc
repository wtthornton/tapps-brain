---
description: Project rules for tapps-brain
globs:
alwaysApply: true
---

# tapps-brain

Persistent cross-session memory system for AI coding assistants. Fully deterministic (no LLM calls), SQLite-backed knowledge store with BM25 ranking, exponential decay, automatic consolidation, cross-project federation, and pluggable vector search.

## Build & Development Commands

```bash
# Install dependencies (uses uv package manager)
uv sync --extra dev

# Install with optional vector search support
uv sync --extra dev --extra vector

# Run all tests (CI-style: exclude benchmarks)
pytest tests/ -v --tb=short -m "not benchmark" --cov=tapps_brain --cov-report=term-missing --cov-fail-under=95

# Production release gate (packaging, version tests, pytest, ruff, mypy, openclaw-plugin)
# Linux / macOS / WSL or Git Bash: bash scripts/release-ready.sh
# OpenClaw doc drift only: python scripts/check_openclaw_docs_consistency.py

# Run a single test file
pytest tests/unit/test_memory_store.py -v

# Lint
ruff check src/ tests/

# Format check
ruff format --check src/ tests/

# Auto-fix lint/format
ruff check --fix src/ tests/
ruff format src/ tests/

# Type check (strict mode)
mypy --strict src/tapps_brain/
```

## Architecture

### Source layout: `src/tapps_brain/`

**Storage layer** — `store.py` is the main `MemoryStore` class: in-memory dict + SQLite write-through, thread-safe via `threading.Lock`. `persistence.py` handles SQLite with WAL mode, FTS5 full-text search, and schema migrations (v1–v11: feedback v9, diagnostics history v10, flywheel v11). MCP server (`mcp_server.py`) exposes **60** tools and **7** resources. See `docs/planning/STATUS.md` for a current snapshot.

**Data model** — `models.py` defines `MemoryEntry` (Pydantic v2) with tier-based classification (`MemoryTier`: architectural/pattern/procedural/context), source tracking, scope visibility, and access counting.

**Retrieval** — `retrieval.py` uses composite scoring: relevance 40%, confidence 30%, recency 15%, frequency 15%. `bm25.py` provides Okapi BM25. `fusion.py` implements Reciprocal Rank Fusion for hybrid search.

**Memory lifecycle** — `decay.py` applies exponential decay with tier-specific half-lives. `consolidation.py` + `auto_consolidation.py` merge memories deterministically using Jaccard + TF-IDF similarity (no LLM). `gc.py` archives stale memories.

**Safety** — `safety.py` detects prompt injection patterns and sanitizes/blocks RAG content.

**Federation** — `federation.py` enables cross-project memory sharing via hub at `~/.tapps-brain/memory/federated.db`.

### Key design decisions

- **Synchronous by design** — no async/await in core code
- **Write-through cache** — all mutations update both in-memory dict and SQLite
- **Lazy decay** — exponential decay computed on read, not via background tasks
- **Deterministic merging** — consolidation uses similarity thresholds, never LLM calls
- **Max 500 entries per project** — enforced in MemoryStore

## Code Quality

- Python 3.12+, strict mypy, ruff with extensive rule set
- Line length: 100 chars
- Tests ignore ANN (annotations) and PLR (pylint refactor) rules
- Coverage minimum: 95%
- LF line endings enforced via `.gitattributes`

## OpenClaw (operators)

- Canonical install/upgrade: `docs/guides/openclaw-runbook.md`
- Full integration guide: `docs/guides/openclaw.md`
- Pre-publish checklist: `scripts/publish-checklist.md` (uses `bash scripts/release-ready.sh`)

## Planning

Epics and stories live in `docs/planning/epics/` with YAML frontmatter. See `docs/planning/PLANNING.md` for format conventions and templates. Reference stories in commits: `feat(story-001.3): description`.

**Shipped product / Cursor (non-Ralph) priority:** `docs/planning/open-issues-roadmap.md` — update this (and GitHub issues) when delivery status changes; do **not** edit `.ralph/` for feature bookkeeping unless explicitly requested. See `docs/planning/PLANNING.md` (section *Open issues roadmap vs Ralph tooling*).

## Ralph (Autonomous Dev Loop)

This project is configured for Ralph — an autonomous development loop that drives Claude Code CLI through tasks iteratively. **`.ralph/` is not packaged** with tapps-brain (PyPI / OpenClaw).

### Ralph Rules

- **Inside a Ralph loop:** `fix_plan.md` is the single source of truth for *which task to run next* in that loop. PROMPT.md defines *how* to work. PROMPT.md must not override fix_plan task order.
- **Outside Ralph:** use `open-issues-roadmap.md` for delivery order; do not modify `.ralph/` unless syncing for Ralph.
- Do ONE task per loop from fix_plan.md, in the order listed.
- Do not skip ahead, reorder, or pick tasks from other sources (epics, specs) unless fix_plan.md explicitly references them.

### Session Startup Requirement (Always)

- **Ralph / autonomous loop:** at the start of each Ralph session, read before planning or edits:
  - `.ralph/fix_plan.md`
  - `.ralph/PROMPT.md`
  - `.ralphrc`
  - `CLAUDE.md`
  - `.ralph/AGENT.md`
- **Cursor or other agents (default):** read `CLAUDE.md` and `docs/planning/open-issues-roadmap.md` when picking delivery work; Ralph files are optional unless you are running Ralph.
- If any file you depend on changes during the session, re-read the changed file(s) before continuing.

### Ralph Files

- `.ralph/PROMPT.md` — Process instructions for the autonomous agent (NOT priorities)
- `.ralph/AGENT.md` — Build/test/lint commands
- `.ralph/fix_plan.md` — **The priority-ordered task list** (top to bottom)
- `.ralph/specs/` — Detailed requirement specs (reference only, not task drivers)
- `.ralph/logs/` — Loop execution logs
- `.ralphrc` — Project-level Ralph configuration

### Important

- **Do not modify** `.ralph/` or `.ralphrc` during a Ralph loop
- Ralph commits its own changes with descriptive messages referencing stories
