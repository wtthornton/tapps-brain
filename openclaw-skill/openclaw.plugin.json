{
  "id": "tapps-brain-memory",
  "name": "tapps-brain — Persistent Memory",
  "description": "ContextEngine plugin for tapps-brain: persistent cross-session memory with BM25 ranking, exponential decay, and multi-agent Hive sharing.",
  "version": "3.7.1",
  "kind": "context-engine",
  "install": {
    "pip": "tapps-brain[mcp]>=3.5.0"
  },
  "mcp": {
    "command": "tapps-brain-mcp",
    "transport": "stdio",
    "args": ["--project-dir", "${workspaceDir}"]
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "mcpCommand": {
        "type": "string",
        "default": "tapps-brain-mcp",
        "description": "Command to spawn the tapps-brain MCP server"
      },
      "profilePath": {
        "type": "string",
        "default": ".tapps-brain/profile.yaml",
        "description": "Path to memory profile YAML config"
      },
      "tokenBudget": {
        "type": "number",
        "default": 3000,
        "description": "Max tokens for injected memories"
      },
      "captureRateLimit": {
        "type": "number",
        "default": 3,
        "description": "Capture facts every N ingest() calls (0 = every call)"
      },
      "agentId": {
        "type": "string",
        "default": "",
        "description": "Unique agent identifier for Hive"
      },
      "hiveEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Enable Hive cross-agent memory sharing"
      },
      "citations": {
        "type": "string",
        "enum": ["auto", "on", "off"],
        "default": "auto",
        "description": "Citation footers in assemble() output: 'auto'/'on' appends 'Source: memory/<tier>/<key>.md', 'off' disables"
      },
      "toolGroups": {
        "oneOf": [
          {
            "type": "string",
            "enum": ["all"],
            "description": "Register all tool groups (default)"
          },
          {
            "type": "array",
            "items": {
              "type": "string",
              "enum": ["core", "lifecycle", "search", "admin", "hive", "federation", "graph"]
            },
            "uniqueItems": true,
            "description": "Register only the listed tool groups"
          }
        ],
        "default": "all",
        "description": "Tool groups to expose as native OpenClaw tools. Groups: core, lifecycle, search, admin, hive, federation, graph. Example: [\"core\",\"lifecycle\",\"search\"] for a coder agent."
      }
    }
  }
}
