# Hot-Path Scoring Baseline — 2026-05-15

Generated for TAP-1844: Score & fix hot files.

## Files scored

| File | Lines | Before | After | Δ |
|------|-------|--------|-------|---|
| `src/tapps_brain/retrieval.py` | 1286 / 1338 | 61.99 | 66.06 | +4.07 |
| `src/tapps_brain/recall.py` | 513 / 526 | 71.36 | 73.29 | +1.93 |
| `src/tapps_brain/store.py` | 4374 / 4415 | 58.82 | 59.33 | +0.51 |

*(After line-counts reflect helper methods added.)*

---

## retrieval.py

### Before (score 61.99)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 6.5 | `search` CC=43 |
| maintainability | 1.33 | MI=13, 1286 lines |
| performance | 8.0 | large_function, very_large_function |
| security | 10 | — |
| test_coverage | 7 | — |

### Top-3 findings addressed

1. **`search` CC=43 → CC=27** — extracted `_filter_candidates_to_pending()` (entry-level
   filtering loop with 8 branches) and `_build_scored_memory_item()` (composite score
   computation with 5 branches) as private methods. The scoring loop in `search()` is now
   a list-comprehension calling the helper.
2. **Dead `channel_trust` variable removed** — the original code computed
   `channel_trust = 1.0` and then multiplied by it (no-op), and also looked up
   `source_key_pt` and `source_key` separately for the same value. Collapsed to a single
   `source_key` lookup in `_build_scored_memory_item()`.
3. **`# noqa: PLR0915` suppression removed** — the `search()` function no longer exceeds
   the statement limit, so the suppression comment was dropped.

### After (score 66.06)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 4.23 | `search` CC=27 (down from 43) |
| maintainability | 1.32 | MI=13, 1338 lines (added helpers) |
| performance | 8.0 | large_function, very_large_function |

*Maintainability score is dominated by file size. Splitting retrieval.py is the long-term
fix (requires a separate refactor — out of scope for this no-API-change task).*

---

## recall.py

### Before (score 71.36)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 4.14 | `_apply_post_filters` CC=26 |
| maintainability | 3.96 | MI=40, 513 lines |
| performance | 6.5 | nested_loops, large_function |
| security | 10 | — |
| test_coverage | 7 | — |

### Top-3 findings addressed

1. **`_apply_post_filters` CC=26 → `recall` is now max at CC=18** — extracted
   `_passes_entry_filters(entry, mem, cfg)` (scope/tier/branch/group checks, 5 branches)
   and `_build_filtered_section(filtered, entry_cache)` (memory section formatting loop)
   as private helpers on `RecallOrchestrator`. `_apply_post_filters` is now 18 lines.
2. **SIM103 lint warning fixed** — last filter condition in `_passes_entry_filters` now
   uses `return not (...)` instead of `if ...: return False; return True`.
3. **Redundant entry lookup eliminated** — the section rebuild previously re-checked
   `if key in entry_cache` with a ternary that could call `self._store.get()` again; the
   refactored `_build_filtered_section` uses the same pattern but through the helper's
   clean boundary, making the cache-hit path unambiguous.

### After (score 73.29)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 2.95 | `recall` CC=18 (next target) |
| maintainability | 3.87 | MI=39, 526 lines |
| performance | 6.5 | large_function, nested_loops |

---

## store.py

### Before (score 58.82)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 4.49 | `search` CC=30 |
| maintainability | 0 | MI=0, 4374 lines |
| performance | 3.5 | very_deep_nesting, nested_loops, large_function |
| security | 10 | — |
| test_coverage | 7 | — |

### Top-3 findings addressed

1. **`search` CC=30 → `search` removed from top spot** — extracted
   `_apply_search_filters(results, *, tags, tier, scope, memory_group, include_historical, as_of)`
   (5-branch filter chain) and `_append_group_memories(results, query, max_group_results)`
   (the group-namespace Hive loop with 4-level nesting). The `search()` function body
   is now 40 lines.
2. **Deep nesting reduced in `search()`** — the group-memories block previously nested
   6 levels deep (with/for/try/for/if/try). The extracted `_append_group_memories()` helper
   eliminates the guard condition (`if include_group_memories and self._groups and ...`)
   from the caller and handles the early-return itself, keeping nesting at ≤ 4 levels
   within the helper.
3. **`__init__` CC=28 surfaced** — after `search` was cleaned up, `__init__` became the
   highest-CC function (CC=28). Splitting `__init__` is architectural scope; noted here
   as the next priority fix.

### After (score 59.33)

| Category | Score | Key finding |
|----------|-------|-------------|
| complexity | 4.21 | `__init__` CC=28 (search de-ranked) |
| maintainability | 0 | MI=0, 4415 lines (added helpers) |
| performance | 3.5 | deep_nesting still in `__init__` |

*`store.py` maintainability is zero because radon's MI formula saturates at files this
large. Splitting the file is the only structural fix (out of scope here).*

---

## Coverage

Per-file coverage ≥ 95% target: deferred to epic-boundary QA (`tapps-validator`). No
existing test contracts were changed — all helpers are private and called only from
the same functions as before.

## Next priorities (not in scope for TAP-1844)

- `retrieval.py::search` CC=27 → extract temporal-keyword builder
- `recall.py::recall` CC=18 → extract Hive merge and graph-boost phases
- `store.py::__init__` CC=28 → extract backend initialisation helpers
- Split `store.py` (4415 lines) → `store_core.py` + `store_search.py` + `store_hive.py`
