{
  "id": "tapps-brain-memory",
  "name": "tapps-brain — Persistent Memory",
  "description": "ContextEngine plugin for tapps-brain: persistent cross-session memory with BM25 ranking, exponential decay, and multi-agent Hive sharing.",
  "version": "3.14.5",
  "minimumVersion": "2026.3.7",
  "kind": "context-engine",
  "slots": {
    "memory": "tapps-brain-memory"
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "mcpCommand": {
        "type": "string",
        "default": "tapps-brain-mcp",
        "description": "Command to spawn the tapps-brain MCP server"
      },
      "profilePath": {
        "type": "string",
        "default": ".tapps-brain/profile.yaml",
        "description": "Path to memory profile YAML config"
      },
      "tokenBudget": {
        "type": "number",
        "default": 2000,
        "description": "Max tokens for injected memories in assemble()"
      },
      "captureRateLimit": {
        "type": "number",
        "default": 3,
        "description": "Run memory_capture every N ingest() calls (0 = every call). Lower N = more capture attempts."
      },
      "agentId": {
        "type": "string",
        "default": "",
        "description": "Unique agent identifier for Hive multi-agent sharing"
      },
      "hiveEnabled": {
        "type": "boolean",
        "default": false,
        "description": "Enable Hive cross-agent memory sharing"
      },
      "citations": {
        "type": "string",
        "enum": ["auto", "on", "off"],
        "default": "auto",
        "description": "Citation footers in assemble() output: 'auto'/'on' appends 'Source: memory/<tier>/<key>.md', 'off' disables"
      }
    }
  }
}
